# MoodAnchor 接线说明

> 目标板：**立创·黄山派（SF32LB52-MOD-1-N16R8）**
> 本文只记录本工程实际用到的连线，完整引脚表见 [SiFli 官方文档](https://wiki.sifli.com/board/sf32lb52x/SF32LB52-%E9%BB%84%E5%B1%B1%E6%B4%BE.html)。
>
> ⚠️ 表中标注 **「需确认」** 的项目尚未实测验证，接线前请核对官方原理图。

---

## 1. 电源与跳线（**优先检查这一节**）

### 1.1 供电方式

| 方式 | 说明 |
|---|---|
| USB Type-C | 板载 CH340N，既供电也用于程序下载与串口调试 |
| 锂电池 | GH-1.25mm 母座，**正向线序**，450~500 mAh 单芯锂电 |

### 1.2 跳线帽（不接会出各种怪问题）

30P 排针上有三组「输出/输入」分离的供电焊盘，**工作时必须用跳线帽短接**：

| 30P 引脚 | 名称 | 必须短接到 | 用途 |
|---|---|---|---|
| 5 | `VBAT_S` | **6** (`VBAT`) | 电池座 → 充电 IC 与模组 VBATS |
| 7 | `VSYS_S` | **8** (`VSYS`) | 充电 IC 输出 → 系统 VSYS |
| 11 | `VCC_3V3_S` | **12** (`VCC_3V3`) | LDO 输出 → 主电 3V3 |

> 设计上把它们断开是为了方便串电流表测功耗，**正常使用必须短接**。

### 1.3 电源相关引脚

| 引脚 | 方向 | 功能 |
|---|---|---|
| `PA_38` | 输出 | VSYS → VSYS_1 切换控制 |
| `PA_26` | 输出 | VSYS_1 → VCC_3V3 切换控制 |
| `PA_30` | 输出 | VSYS_1 → HR3V3 切换控制（**传感器电源**） |
| `PA_44` | 输入 | `VBUS_DET`，充电器插入检测 |
| 模组 61 脚 | 输入 | `VBATS`，电池电压检测输入 |

### 1.4 充电 IC

| 项 | 值 |
|---|---|
| 总线 | I²C0（`PA_10` = SCL，`PA_11` = SDA） |
| 地址 | `0x49` |

⚠️ **需确认**：官方文档特性列表写的是 **SY6103**，而板上 BSP 代码（`bsp_power.c`）按 **AW32001** 的寄存器映射操作（`0x01` bit3 = 充电使能，`0x05` bit5-6 = 看门狗）。两者寄存器并不通用，若要自己改充电参数，请先确认实际器件。

### 1.5 关于电池电压读数

`VBATS`（模组 61 脚）经充电 IC 的 `VBAT` 节点接入 `ADC_CHAN_5`。

⚠️ **插着 USB 时该节点会被充电 IC 钳位，读数不代表电池**。因此固件在检测到 USB 插入时显示 `CHARGING`，只有纯电池供电时才显示 `xx%(x.xxV)`。

⚠️ **需确认**：目前实测（插 USB）恒读约 3.3 V。拔掉 USB 用电池后的读数**尚未验证**，若仍为 3.3 V 则说明该采样链路另有问题。

---

## 2. 无需接线的板载资源

| 器件 | 接口 / 引脚 | 工程中的使用 |
|---|---|---|
| AMOLED 1.85" 390×450（CO5300AF-01） | 22P FPC，QSPI | `/dev/fb0`、LVGL 显示 |
| 触摸 FT6146-M00 | I²C，INT=`PA_41`，RST=`PA_09` | `/dev/input0` |
| 六轴 IMU LSM6DS3TR-C | I²C1，INT=`PA_31` | `/dev/lsm6dsl0` |
| 地磁 MMC5603NJ | I²C1 | 未使用 |
| 环境光 LTR-303ALS-01 | I²C1 | 未使用 |
| RGB LED SK6812 | `PA_32` | 未使用 |
| MEMS 麦克风 | 模组 36 脚 `MIC_BIAS` / 37 脚 `MIC_ADC_IN` | 未使用（走芯片内部 AUDCODEC） |
| 按键 KEY1 | `PA_34`（开关机 / 长按 10 s 复位） | `/dev/buttons` |
| 按键 KEY2 | `PA_43`（功能键） | `/dev/buttons` |
| 马达驱动电路 | `PA_20`（PWM），**预留焊接点** | `/dev/gpio3` |

---

## 3. 需要外接的传感器

### 3.0 取电与接地（**先看这一节，接错会当场黑屏**）

| 用途 | 30P 引脚 | 说明 |
|---|---|---|
| **3.3 V 供电** | **12** (`VCC_3V3_S` / `VCC_3V3`) | 传感器唯一正确的取电点 |
| **地** | **3 / 4** (`GND`) | |

⚠️ **绝对不要从下面这些引脚取电给传感器**：

| 30P 引脚 | 名称 | 为什么不行 |
|---|---|---|
| 1 / 2 | `USB_VBUS_5V` | **5 V**，3.3 V 器件会直接损坏 |
| 5 | `VBAT_S` | 电池座侧，3.0~4.2 V，且是**电流表断点** |
| 6 | `VBAT` | 充电 IC / 模组 VBATS 侧，同样是断点 |
| 7 | `VSYS_S` | 充电 IC 输出侧，约 4 V，**断点** |
| 8 | `VSYS` | 系统侧，**断点** |

**5/6/7/8 这四脚是为「串电流表测功耗」而故意断开的电源节点。** 从上面取电，等于把传感器的负载直接挂到电池或 VSYS 上，会拉低整个系统的供电 —— 现象就是**屏幕上电即黑（或一接近传感器就黑）**。

⚠️ **Grove 线序有正反之分**（M5Stack 为正向，Seeed 为反向）。接线前务必对照传感器模块丝印确认 VCC / GND，**接反 = 直接短路**，现象同样是屏幕瞬间变黑。

---

### 3.1 GSR 皮肤电（情绪检测主传感器）

| 项 | 值 |
|---|---|
| 传感器 | **Grove GSR 模块**（M5Stack 兼容） |
| 接到 | **`PA_28`** → **30P 排针第 21 脚** |
| 内核设备 | `/dev/adc1`（`ADC1` 通道 0） |
| 代码 | `hs_gsr_open() / hs_gsr_read()` |

**接线要点：**

- `PA_28` 在官方定义里是 **SPI1_CLK（TF 卡）**。本工程**禁用了 SPI1/TF 卡**，把它复用为 ADC 输入给 GSR。
  → **接了 GSR 就不能插 TF 卡**。
- Grove GSR 模块上有一个**电位器**，需要按下面步骤校准：
  1. 电极**悬空**
  2. 运行 `huangshan_hal_demo gsr_cal 30`，记下输出的 `CAL_RAW10`
  3. 运行 `huangshan_hal_demo gsr_stream <CAL_RAW10>`
- 采集速率约 **5 Hz**（GSR 是慢变信号，够用）。
- **输出方向（容易判反）**：模块用恒流驱动电极，**接触越好电压越低**。
  本板实测 **戴上 < 500 mV、悬空约 2000 mV**，所以固件把
  `> 1500 mV` 判为"未佩戴"（`MA_GSR_NO_ELECTRODE_MV`）。
- **供电见 §3.0**：3.3 V 取自 **30P-12**，地取自 **30P-3/4**。
  `PA_30` 只是**传感器电源域的控制信号**（VSYS_1 → HR3V3 切换），固件会拉高它给板载 IMU / MAX30102 供电；**它不是电源引脚，不能从这里取电**。

### 3.2 MAX30102 心率 / 血氧

| 项 | 值 |
|---|---|
| 传感器 | MAX30102（兼容 MAX30105 的模块也行） |
| 接到 | **I²C1**：SCL = **30P-16 (`PA_40`)**，SDA = **30P-14 (`PB_39`)** ⚠️ |
| I²C 地址 | **`0x57`** |
| 内核设备 | `/dev/i2c1` |
| 代码 | `hs_max30102_open() / hs_max30102_read_sample()` + `hs_ppg.c` 算法 |

**接线要点：**

- **供电见 §3.0**：3.3 V 取自 **30P-12**，地取自 **30P-3/4**。
- ⚠️ **注意引脚差异**：板载 IMU 用的是模组内部的 `PA_39`（SDA），而 30P 排针上引出的是 `PB_39`（30P-14）。两者**不是同一个物理引脚**。外部设备能否与板载 IMU 共用这条总线**尚未实测确认**；如果 `/dev/i2c1` 上读不到 `0x57`，优先怀疑这一点。
- `INT` 引脚**可以不接**：驱动按 100 Hz 轮询 FIFO，不依赖中断。
- 传感器配置由固件完成：SpO2 模式（RED+IR）、100 samples/s、脉宽 411 µs、LED 电流 7.2 mA、FIFO rollover 使能。
- 测心率时需要**手指贴紧**传感器窗口，等待 5~10 秒。

---

## 4. 调试与烧录接口

| 项 | 值 |
|---|---|
| 调试 UART | `PA_19` = TXD，`PA_18` = RXD → CH340N → USB Type-C |
| 串口参数 | **1 000 000 8N1** |
| 复位 | CH340N 的 `RTS#` 拉低再拉高（低有效） |
| 下载模式 | 勾选 BOOT 后上电 |
| TF 卡 | SPI1（`PA_24/25/27/28/29`）—— 其中 `PA_28` 被 GSR 占用 |

⚠️ **打开串口会通过 RTS 复位板子**，无法做到「不复位地监听」。

---

## 5. 设备节点 ↔ 引脚对照

| 设备节点 | 引脚 / 通道 | 用途 | 代码入口 |
|---|---|---|---|
| `/dev/adc0` | `ADC_CHAN_5` = VBATS | 电池电压（插 USB 时被钳位） | `hs_adc_read(HS_ADC_VBAT_CHANNEL)` |
| `/dev/adc1` | `ADC_CHAN_0` = `PA_28` | GSR | `hs_gsr_read()` |
| `/dev/i2c0` | `PA_10` / `PA_11` | 充电 IC（`0x49`） | `hs_i2c_open(0)` |
| `/dev/i2c1` | 板载 IMU 用 `PA_39`/`PA_40`；30P 引出 `PB_39`(14) / `PA_40`(16) | IMU + MAX30102 | `hs_imu_*` / `hs_max30102_*` |
| `/dev/gpio1` | `PA_44` | VBUS 检测 | `ma_usb_present()` |
| `/dev/gpio3` | `PA_20` | 振动马达 | `hs_vibration_set()` |
| `/dev/lsm6dsl0` | I²C1 | 六轴 IMU | `hs_imu_read()` |
| `/dev/buttons` | `PA_34` / `PA_43` | 按键 | `hs_buttons_read()` |
| `/dev/fb0` | QSPI | 显示 | LVGL |
| `/dev/input0` | 触摸 I²C | 触摸屏 | LVGL |

---

## 6. 快速自查清单

上电前：

- [ ] **30P 跳线帽 5-6 / 7-8 / 11-12 都已短接**
- [ ] 电池正负极与丝印一致（GH-1.25 正向线序）
- [ ] **传感器 3.3 V 取自 30P-12，地取自 30P-3/4**；**没有用 1/2/5/6/7/8 脚取电**
- [ ] **Grove 线序与模块丝印一致**（VCC / GND 没有接反）
- [ ] GSR 接 `PA_28`（30P-21），且**没有插 TF 卡**
- [ ] MAX30102：SCL = 30P-16 (`PA_40`)，SDA = 30P-14 (`PB_39`)，地址 `0x57`
- [ ] 调试串口 1 000 000 8N1

上电后，看首页（第 1 页 SYSTEM）的传感器在线列表：

```
GSR     adc1     OK
PPG     i2c1     OK
IMU     lsm6d    OK
BUTTON  btn      OK
MOTOR   gpio3    OK
TOUCH   input0   OK
```

`OK` = 节点存在且有数据；`IDLE` = 节点在但还没采到数据；`NO NODE` = 设备没注册。
